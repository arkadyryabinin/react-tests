// Clone Arrays of Objects

const initialList = [
  { id: 0, title: 'Big Bellies', seen: false },
  { id: 1, title: 'Lunar Landscape', seen: false },
  { id: 2, title: 'Terracotta Army', seen: true },
];

// use Array.map
const cloneList = initialList.map((item) => ({ ...item }));